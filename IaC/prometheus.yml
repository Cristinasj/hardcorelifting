global:
  scrape_interval: 15s  # How often to scrape targets

scrape_configs:
  - job_name: 'backend'
    static_configs:
      - targets: ['backend:8080']  # Replace with your service name & port
  
  - job_name: 'nginx'
    static_configs:
      - targets: ['nginx-exporter:9113']
